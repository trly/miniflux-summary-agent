[project]
name = "miniflux-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "miniflux>=1.1.4",
    "ollama>=0.5.1",
]
